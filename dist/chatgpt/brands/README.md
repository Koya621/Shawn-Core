# 品牌卡上传说明

这里是从 `source/brands/` 同步出来的网页 GPT 上传副本。

维护时请改 `source/brands/`，再同步到这里。

上传 GPT 时建议：

1. 先上传 `knowledge-shawn-core.md`。
2. 再上传当前要用的品牌 `brand-card.md`。
3. 最后上传该品牌 logo 和 3-10 张优秀案例。

不要一次性上传大量无筛选素材，避免 GPT 检索混乱。
